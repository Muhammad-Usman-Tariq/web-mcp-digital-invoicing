HOST=0.0.0.0
PORT=8000
LOG_LEVEL=INFO

# --- Central Auth (paste exact values from Central Auth admin panel) ---
JWKS_URI=
MCP_AUTH_AUDIENCE=
REVOCATIONS_URI=

# --- Supabase (paste from Supabase project settings) ---
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# --- Cryptography (generate via: python scripts/generate_master_key.py) ---
ENCRYPTION_MASTER_KEY=
CURRENT_KEY_VERSION=1

# --- Browser Automation (safe defaults, override only if needed) ---
PORTAL_BASE_URL=https://www.digitalinvoicingsoftware.com
HEADLESS=true
MAX_CONCURRENT_BROWSERS=3
NAVIGATION_TIMEOUT_MS=20000
ACTION_TIMEOUT_MS=15000
SESSION_TTL_HOURS=24
